{
  "manifest_version": 3,
  "name": "Paprika Agent",
  "version": "0.2.0",
  "description": "Built-in Paprika worker helper. Exposes a generic command bus for Chrome capabilities that CDP / nodriver can't reach directly (genuine page zoom, etc.). Driven by the worker via a page<->content-script<->service-worker relay; has no UI.",
  "permissions": [
    "tabs"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "all_frames": false,
      "run_at": "document_start",
      "js": ["content.js"]
    }
  ]
}
