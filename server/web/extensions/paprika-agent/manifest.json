{
  "manifest_version": 3,
  "name": "Paprika Agent",
  "version": "0.1.0",
  "description": "Built-in Paprika worker helper. Exposes a generic command bus for Chrome capabilities that CDP / nodriver can't reach directly (genuine page zoom, etc.). Driven by the worker over CDP; has no UI.",
  "permissions": [
    "tabs"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  }
}
